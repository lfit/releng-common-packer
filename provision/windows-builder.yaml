---

- hosts: all

  tasks:
    - name: Install Tools
      win_chocolatey:
        name: '{{pkg}}'
        state: present
      with_items:
        - 7zip
        - curl
        - git
        - openssh
        - python2
        - python3
        - vim
        - wget
      loop_control:
        loop_var: pkg
